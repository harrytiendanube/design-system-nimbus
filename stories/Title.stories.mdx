import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { action } from "@storybook/addon-actions";

import { Title, Link } from "../src/components";
import "./utils/style.css";

<Meta title="Atoms/Title" component={Title} />

# Title

Utiliza los botones para acciones que no requieran cambios en la URL.

<Preview>
  <Story name="base" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Title>{text("Texto", "Texto por defecto")}</Title>
  </Story>
</Preview>

<Props of={Title} />

## Conteniendo componentes

<Preview withToolbar>
  <Story name="componentes">
    <div className="separate">
      <Title>
        {" "}
        Título con Link al final <Link
          onClick={action("Click en acceder a mi tienda")}
        >
          acceder a mi tienda
        </Link> desde el admin
      </Title>
    </div>
  </Story>
</Preview>
