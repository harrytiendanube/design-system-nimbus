import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { action } from "@storybook/addon-actions";

import { Text, Link } from "../src/components";
import "./utils/style.css";

<Meta title="Atoms/Text" component={Text} />

# Text

Utiliza los botones para acciones que no requieran cambios en la URL.

<Preview>
  <Story name="base" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Text>{text("Texto", "Texto por defecto")}</Text>
  </Story>
</Preview>

<Props of={Text} />

## Conteniendo componentes

<Preview withToolbar>
  <Story name="componentes">
    <div className="separate">
      <Text size="small">
        {" "}
        Texto con Link al final <Link
          onClick={action("Click en acceder a mi tienda")}
        >
          acceder a mi tienda
        </Link> desde el admin
      </Text>
    </div>
  </Story>
</Preview>

## Tamaños

<Preview withToolbar>
  <Story name="tamaños">
    <div className="separate">
      <Text size="small">Tengo pequeño</Text>
      <Text size="regular">Tengo regular</Text>
    </div>
  </Story>
</Preview>
