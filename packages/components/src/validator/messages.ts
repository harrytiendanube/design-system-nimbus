const messages: { [key: string]: string } = {
  required: "This field is required",
  number: "This value only can be a number",
  string: "This value only can be string",
  email: "This value only can be email",
  maxLength: "Max length is %1",
  minLength: "Min length is %1",
  pattern: "Error in pattern",
};

export default messages;
