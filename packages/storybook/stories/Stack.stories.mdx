import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";

import { Stack, Label, Title, Button } from "@tiendanube/components/src";
import "../utils/style.css";

<Meta title="Composite components/Stack" component={Stack} />

# Stack

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    {() => {
      return (
        <Stack
          justify={select("justify", [
            "flex-start",
            "center",
            "flex-end",
            "space-between",
          ])}
          align={select("align", [
            "flex-start",
            "center",
            "flex-end",
            "stretch",
          ])}
          spacing={select("spacing", ["base", "tight", "loose", "none"])}
          column={boolean("column", false)}
          wrap={boolean("wrap", false)}
        >
          <Button onClick={action("onClick")}>Button</Button>
          <Button onClick={action("onClick")}>Button</Button>
          <Button onClick={action("onClick")}>Button</Button>
          <Label id="label" label="Label" />
          <Button onClick={action("onClick")}>Button</Button>
          <Button onClick={action("onClick")}>Button</Button>
          <Button onClick={action("onClick")}>Button</Button>
        </Stack>
      );
    }}
  </Story>
</Preview>

## Properties Stack

<Props of={Stack} />

## Properties Stack.Item

<Props of={Stack.Item.type} />

# Stack with Stack Item

TODO: complete

<Preview>
  <Story
    name="with stack items"
    parameters={{ decorators: [withKnobs, withA11y] }}
  >
    {() => {
      return (
        <Stack
          justify={select("justify", [
            "flex-start",
            "center",
            "flex-end",
            "space-between",
          ])}
          align={select("align", [
            "flex-start",
            "center",
            "flex-end",
            "stretch",
          ])}
          spacing={select("spacing", ["base", "tight", "loose", "none"])}
          column={boolean("column", false)}
          wrap={boolean("wrap", false)}
        >
          <Stack.Item fill={boolean("fill", false)}>
            <Title>Main title</Title>
          </Stack.Item>
          <Stack.Item>
            <Label id="label" label="Label" />
          </Stack.Item>
        </Stack>
      );
    }}
  </Story>
</Preview>
