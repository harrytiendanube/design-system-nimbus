import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";
import { Alert } from '../../components/src';

import '../utils/style.css';

<Meta title="Atoms/Alert" component={Alert} />

# Alert

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Alert
      type={select("type", ["inline","toast"])}
      isDismissable={boolean("isDismissable", true)}
      onDismiss={action("onDismiss")}
      show={boolean("show", true)}
      appearance={select("appearance", ["primary", "secondary", "danger", "warning", "success"])}
      title={text("title", "Title")}
      primaryLabel={text("primaryLabel", "Primary label")}
      onClickPrimary={action("onClickPrimary")}
      secondaryLabel={text("secondaryLabel", "Secondary Label")}
      secondaryTo={text("secondaryTo", "http://www.tiendanube.com")}
    >
    {text("children", "Text")} </Alert>
  </Story>
</Preview>

## Properties

<Props of={Alert.type} />

## Appearances

TODO: complete

<Preview withToolbar>
  <Story name="appearances">
    <div className="separate">
      <Alert show>Default State</Alert>
      <Alert appearance="primary" show>Primary State</Alert>
      <Alert appearance="danger" show>Danger State</Alert>
      <Alert appearance="warning" show>Warning State</Alert>
      <Alert appearance="success" show>Success State</Alert>
    </div>
  </Story>
</Preview>