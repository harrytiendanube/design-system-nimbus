import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";
import { Alert } from "@tiendanube/components/src";
import { LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atoms/Alert" component={Alert} />

# Alert

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Alert
      isDismissable={boolean("isDismissable", true)}
      onDismiss={action("onDismiss")}
      show={boolean("show", true)}
      appearance={select("appearance", [
        "primary",
        "secondary",
        "danger",
        "warning",
        "success",
      ])}
      title={text("title", "Title")}
      primaryLabel={text("primaryLabel", "Primary label")}
      onClickPrimary={action("onClickPrimary")}
      secondaryLabel={text("secondaryLabel", "Secondary Label")}
      secondaryTo={text("secondaryTo", "http://www.tiendanube.com")}
    >
      {text("children", "Text")}{" "}
    </Alert>
  </Story>
</Preview>

## Properties

<Props of={Alert.type} />

## Appearance

TODO: complete

<Preview withToolbar>
  <Story name="appearance">
    <div className="separate">
      <Alert show>Default State</Alert>
      <Alert appearance="primary" show>
        Primary State
      </Alert>
      <Alert appearance="danger" show>
        Danger State
      </Alert>
      <Alert appearance="warning" show>
        Warning State
      </Alert>
      <Alert appearance="success" show>
        Success State
      </Alert>
    </div>
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview withToolbar>
  <Story name="stressed">
    <Alert
      title="Title"
      primaryLabel="Primary label"
      secondaryLabel="Secondary Label"
      isDismissable
      show
    >
      {LOREM_STRESS}
    </Alert>
  </Story>
</Preview>
