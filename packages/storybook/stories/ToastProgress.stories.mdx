import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean, number } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";

import { ToastProgress } from "@tiendanube/components/src";

<Meta title="Atomic components/ToastProgress" component={ToastProgress} />

# ToastProgress

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    {() => {
      return (
        <>
          <ToastProgress
            label={text("label", "label")}
            percentage={number("percentage", "")}
            show={boolean("show", false)}
          />
        </>
      );
    }}
  </Story>
</Preview>

## Properties

<Props of={ToastProgress.type} />
