import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, color } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";
import { Label, Text, Stack } from "@tiendanube/components/src";
import { LABEL_APPEARANCES_ICONS, LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atomic components/Label" component={Label} />

# Label

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Label
      id="id-1"
      icon={LABEL_APPEARANCES_ICONS[0].icon}
      label={text("label", "Text")}
    />
  </Story>
</Preview>

## Properties

<Props of={Label} />
<Text>
  - Parameters <Text appearance="primary">icon </Text> and{" "}
  <Text appearance="primary"> colorTag </Text> parameters are mutually exclusive
</Text>
<Text>
  - Parameters <Text appearance="primary">appearance </Text> and{" "}
  <Text appearance="primary"> colorTag </Text> parameters are mutually exclusive
</Text>

# Skeleton

<Preview>
  <Story name="skeleton" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Label.Skeleton />
  </Story>
</Preview>

# Appearance

TODO: complete

<Preview withToolbar>
  <Story name="appearance">
    {() => (
      <Stack column align="stretch">
        <Stack.Item>
          <Stack wrap>
            {LABEL_APPEARANCES_ICONS.map(({ appearance }, index) => (
              <Stack.Item key={index}>
                <Label
                  id={`id-${index}`}
                  appearance={appearance}
                  label={appearance}
                />
              </Stack.Item>
            ))}
          </Stack>
        </Stack.Item>
        <Stack.Item>
          <Stack wrap>
            {LABEL_APPEARANCES_ICONS.map(({ appearance }, index) => (
              <Stack.Item key={index}>
                <Label
                  id={`id-${index}`}
                  appearance={appearance}
                  label={appearance}
                  onClick={action("onClick")}
                />
              </Stack.Item>
            ))}
          </Stack>
        </Stack.Item>
      </Stack>
    )}
  </Story>
</Preview>

# Appearance and icon

TODO: complete

<Preview withToolbar>
  <Story name="appearance and icon">
    {() => (
      <Stack column align="stretch">
        <Stack.Item>
          <Stack wrap>
            {LABEL_APPEARANCES_ICONS.map(({ appearance, icon }, index) => (
              <Stack.Item key={index}>
                <Label
                  id={`id-${index}`}
                  appearance={appearance}
                  label={appearance}
                  icon={icon}
                />
              </Stack.Item>
            ))}
          </Stack>
        </Stack.Item>
        <Stack.Item>
          <Stack wrap>
            {LABEL_APPEARANCES_ICONS.map(({ appearance, icon }, index) => (
              <Stack.Item key={index}>
                <Label
                  id={`id-${index}`}
                  appearance={appearance}
                  label={appearance}
                  icon={icon}
                  onClick={action("onClick")}
                />
              </Stack.Item>
            ))}
          </Stack>
        </Stack.Item>
      </Stack>
    )}
  </Story>
</Preview>

## Color tag

<Preview>
  <Story name="color tag" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Stack wrap>
      <Stack.Item>
        <Label
          id="id-1"
          colorTag={color("colorTag1", "#ff0000")}
          label="Label without action"
        />
      </Stack.Item>
      <Stack.Item>
        <Label
          id="id-2"
          colorTag={color("colorTag2", "#4a90e2")}
          label="Label with action"
          onClick={action("onClick")}
        />
      </Stack.Item>
    </Stack>
  </Story>
</Preview>

## Stressed

<Preview>
  <Story name="stressed" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Label id="id-1" label={LOREM_STRESS} />
  </Story>
</Preview>
