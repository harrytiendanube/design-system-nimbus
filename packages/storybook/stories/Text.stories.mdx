import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, select, boolean } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";

import { Text } from "@tiendanube/components/src";
import { LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atomic components/Text" component={Text} />

# Text

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Text
      appearance={select("appearance", [
        "default",
        "primary",
        "secondary",
        "success",
        "warning",
        "danger",
        "light",
      ])}
      bold={boolean("bold", false)}
      block={boolean("block", false)}
      size={select("size", ["base", "small", "featured"])}
      background={boolean("background", false)}
    >
      {text("children", "Text")}
    </Text>
    <Text>Another Text</Text>
  </Story>
</Preview>

## Properties

<Props of={Text} />

## Properties Text.Skeleton

<Props of={Text.Skeleton.type} />

## Properties Text.Span

<Props of={Text.Span.type} />

# Skeleton

<Preview>
  <Story name="skeleton" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Text.Skeleton
      width={select("width", ["small", "medium", "large", "fill"])}
      size={select("size", ["small", "base", "featured"])}
    />
  </Story>
</Preview>

# Span

<Preview>
  <Story name="span" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Text
      appearance={select("appearance", [
        "default",
        "primary",
        "secondary",
        "success",
        "warning",
        "danger",
        "light",
      ])}
      bold={boolean("bold", false)}
      block={boolean("block", false)}
      size={select("size", ["base", "small", "featured"])}
      background={boolean("background", false)}
    >
      {text("Text content", "Text content")}
      <Text.Span
        appearance={select("span appearance", [
          "default",
          "primary",
          "secondary",
          "success",
          "warning",
          "danger",
          "light",
        ])}
        bold={boolean("span bold", false)}
        size={select("span size", ["base", "small", "featured"])}
        background={boolean("span background", false)}
      >
        {text("Span content", "Span content")}
      </Text.Span>
    </Text>
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview>
  <Story name="stressed" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Text>{LOREM_STRESS}</Text>
  </Story>
</Preview>
