import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { Layout, Text, Card } from "@tiendanube/components/src";
import { LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Layout/Layout" component={Layout} />

# Layout

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Layout
      appearance={select("Appearance", [
        "fluid",
        "centered",
        "halves",
        "aside",
      ])}
    >
      <Layout.Section isMain={boolean("isMain", false)}>
        <Card title={text("Left column card title", "Left column card title")}>
          <Text>{LOREM_STRESS}</Text>
        </Card>
      </Layout.Section>
      <Layout.Section>
        <Card
          title={text("Right column card title", "Right column card title")}
        >
          <Text>{LOREM_STRESS}</Text>
        </Card>
      </Layout.Section>
    </Layout>
  </Story>
</Preview>

## Properties Layout

<Props of={Layout} />

## Properties Layout.Section

<Props of={Layout.Section.type} />
