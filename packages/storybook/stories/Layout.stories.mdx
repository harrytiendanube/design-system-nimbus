import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";

import { Layout, Text } from '@tiendanube/components/src';

import '../utils/style.css';

<Meta title="Components/Layout" component={Layout} />

# Layout

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Layout appearance={select("Appearance", ["fluid", "centered", "halves", "aside"])}>
        <section>
          <Text>{text("Left column", "Content")}</Text>
        </section>
        <section>
          <Text>{text("Right column", "Content")}</Text>
        </section>
    </Layout>
  </Story>
</Preview>

<Props of={Layout.type} />