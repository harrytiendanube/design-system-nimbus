import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";

import {
  TextSizeIcon,
  BoldIcon,
  ItalicIcon,
  AlignLeftIcon,
  AlignCenterIcon,
  AlignRightIcon,
  DropIcon,
  UndoIcon,
  RedoIcon,
  OrderedListIcon,
  ListIcon,
  RemoveFormatIcon,
} from "@tiendanube/icons";
import { KeyboardToolbar, Button, Input } from "@tiendanube/components/src";

import "../utils/style.css";

<Meta title="Patterns/KeyboardToolbar" component={KeyboardToolbar} />

# KeyboardToolbar

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    {() => {
      const [show, setShow] = React.useState(false);
      const toggleShow = () => setShow((currentShow) => !currentShow);
      return (
        <>
          <Button onClick={toggleShow}>Show Toolbar</Button>
          <Input
            name="test-input"
            placeholder="Focus the input to test the toolbar"
            label="HTML Editor"
            onFocus={() => setShow(true)}
            onBlur={() => setShow(false)}
            multiRows
            rows={5}
          />
          <KeyboardToolbar show={show}>
            <KeyboardToolbar.Group>
              <KeyboardToolbar.Popover
                icon={TextSizeIcon}
                onClick={action("onclick")}
                label="aria label"
                column
              >
                <KeyboardToolbar.Button
                  onClick={action("onclick")}
                  label="Heading"
                />
                <KeyboardToolbar.Button
                  onClick={action("onclick")}
                  label="Title"
                  disabled
                />
                <KeyboardToolbar.Button
                  onClick={action("onclick")}
                  label="Subtitle"
                />
                <KeyboardToolbar.Button
                  onClick={action("onclick")}
                  label="Normal"
                  active
                />
              </KeyboardToolbar.Popover>
              <KeyboardToolbar.Button
                icon={BoldIcon}
                onClick={action("onclick")}
                label="aria label"
                active
              />
              <KeyboardToolbar.Button
                icon={ItalicIcon}
                onClick={action("onclick")}
                label="aria label"
                active
              />
              <KeyboardToolbar.Button
                icon={DropIcon}
                onClick={action("onclick")}
                label="aria label"
                active
              />
            </KeyboardToolbar.Group>
            <KeyboardToolbar.Group>
              <KeyboardToolbar.Popover
                icon={AlignLeftIcon}
                onClick={action("onclick")}
                label="aria label"
              >
                <KeyboardToolbar.Button
                  icon={AlignLeftIcon}
                  onClick={action("onclick")}
                  label="aria label"
                  active
                />
                <KeyboardToolbar.Button
                  icon={AlignCenterIcon}
                  onClick={action("onclick")}
                  label="aria label"
                />
                <KeyboardToolbar.Button
                  icon={AlignRightIcon}
                  onClick={action("onclick")}
                  label="aria label"
                />
              </KeyboardToolbar.Popover>
            </KeyboardToolbar.Group>
            <KeyboardToolbar.Group>
              <KeyboardToolbar.Button
                icon={UndoIcon}
                onClick={action("onclick")}
                label="aria label"
                active
              />
              <KeyboardToolbar.Button
                icon={RedoIcon}
                onClick={action("onclick")}
                label="aria label"
                disabled
              />
              <KeyboardToolbar.Button
                icon={RemoveFormatIcon}
                onClick={action("onclick")}
                label="aria label"
                active
              />
            </KeyboardToolbar.Group>
            <KeyboardToolbar.Group>
              <KeyboardToolbar.Popover
                icon={ListIcon}
                onClick={action("onclick")}
                label="aria label"
              >
                <KeyboardToolbar.Button
                  icon={ListIcon}
                  onClick={action("onclick")}
                  label="aria label"
                />
                <KeyboardToolbar.Button
                  icon={OrderedListIcon}
                  onClick={action("onclick")}
                  label="aria label"
                />
              </KeyboardToolbar.Popover>
            </KeyboardToolbar.Group>
          </KeyboardToolbar>
        </>
      );
    }}
  </Story>
</Preview>

## Properties

### KeyboardToolbar

<Props of={KeyboardToolbar} />

### KeyboardToolbar.Group

<Props of={KeyboardToolbar.Group} />

### KeyboardToolbar.Popover

<Props of={KeyboardToolbar.Popover} />

### KeyboardToolbar.Button

<Props of={KeyboardToolbar.Button} />
