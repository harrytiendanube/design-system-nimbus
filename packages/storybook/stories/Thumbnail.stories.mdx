import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import {
  withKnobs,
  select,
  boolean,
  text,
  number,
} from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";
import { CameraIcon } from "@tiendanube/icons";

import { Thumbnail } from "@tiendanube/components/src";
import { images } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atomic components/Thumbnail" component={Thumbnail} />

# Thumbnail

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      src={select("src", images, images.image1)}
      altText={text("alt text", "Alternative text")}
      loading={boolean("loading", false)}
    />
  </Story>
</Preview>

## Properties

<Props of={Thumbnail} />

# Empty

<Preview>
  <Story name="empty" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Empty
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      placeholderIcon={CameraIcon}
    />
  </Story>
</Preview>

## Empty Properties

<Props of={Thumbnail.Empty} />

# Select

<Preview>
  <Story name="select0" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Select
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      src={select("src", images, images.image1)}
      altText={text("alt text", "Alternative text")}
      id="thumbnail-id"
      onSelect={action("onSelect")}
    />
  </Story>
</Preview>

## Select Properties

<Props of={Thumbnail.Select} />

# Skeleton

<Preview>
  <Story name="skeleton" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Skeleton
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
    />
  </Story>
</Preview>

## Skeleton Properties

<Props of={Thumbnail.Skeleton} />

# Add

<Preview>
  <Story name="add" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Add
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      onClick={action("add")}
      ariaLabel={text("aria label", "aria label")}
      disabled={boolean("disabled", false)}
      helperText={text("helper text", "")}
    />
  </Story>
</Preview>

## Add Properties

<Props of={Thumbnail.Add} />

# File

<Preview>
  <Story name="file" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.File
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      ariaLabel={text("aria label", "aria label")}
      open={boolean("open", false)}
      disabled={boolean("disabled", false)}
      onChange={action("onChange")}
      multiple={boolean("multiple", true)}
      helperText={text("helper text", "")}
    />
  </Story>
</Preview>

## File Properties

<Props of={Thumbnail.File} />

# Plus

<Preview>
  <Story name="plus" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Plus
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      src={select("src", images, images.image1)}
      altText={text("alt text", "Alternative text")}
      ariaLabel={text("aria label", "aria label")}
      number={number("number", 5)}
      onClick={action("plus")}
    />
  </Story>
</Preview>

## Plus Properties

<Props of={Thumbnail.Plus} />

# Error

<Preview>
  <Story name="error" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Thumbnail.Error
      aspectRatio={select("aspect ratio", [
        "1-1",
        "16-9",
        "9-16",
        "4-3",
        "3-4",
        "2-1",
        "1-2",
      ])}
      width={number("width", 104)}
      src={select("src", images, images.image1)}
      ariaLabel={text("aria label", "aria label")}
      onClick={action("error")}
      altText={text("alt text", "Alternative text")}
    />
  </Story>
</Preview>

## Error Properties

<Props of={Thumbnail.Error} />
