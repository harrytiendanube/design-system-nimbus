import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, select } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";

import { ImageItem } from "@tiendanube/components/src";
import {
  LOREM_STRESS,
  IMAGE_PRODUCT,
  IMAGE_APPEARANCES,
} from "./utils/constants";

<Meta title="Atoms/ImageItem" component={ImageItem} />

# ImageItem

TODO: Complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <ImageItem
      thumbnail={IMAGE_PRODUCT}
      link={{
        children: text("link.children", "Product"),
        onClick: action("link.OnClick"),
      }}
      subtitle={{
        children: text("subtitle.children", "Variant"),
        appearance: select("subtitle.appearance", IMAGE_APPEARANCES),
      }}
      description={{
        children: text("description.children", "1 x $3.500"),
        appearance: select("description.appearance", IMAGE_APPEARANCES),
      }}
    />
  </Story>
</Preview>

## Properties

<Props of={ImageItem} />

# Skeleton

<Preview>
  <Story name="skeleton" parameters={{ decorators: [withKnobs, withA11y] }}>
    <ImageItem.Skeleton />
  </Story>
</Preview>

## without thumbnail

TODO: complete

<Preview>
  <Story name="without thumbnail">
    <ImageItem
      link={{ children: "Product", onClick: action("link.OnClick") }}
      subtitle={{ children: "Variant" }}
      description={{ children: "1 x $3.500" }}
    />
  </Story>
</Preview>

## with non-existent thumbnail

TODO: complete

<Preview>
  <Story name="non-existent thumbnail">
    <ImageItem
      thumbnail="http://thisURLsDoesNotExists.com"
      link={{
        children: text("link.children", "Product"),
        onClick: action("link.OnClick"),
      }}
      subtitle={{ children: "Variant" }}
      description={{ children: "1 x $3.500" }}
    />
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview>
  <Story name="stressed">
    <ImageItem
      thumbnail={IMAGE_PRODUCT}
      link={{
        children: LOREM_STRESS,
        onClick: action("link.OnClick"),
      }}
      subtitle={{ children: LOREM_STRESS }}
      description={{ children: "1 x $3.500" }}
    />
  </Story>
</Preview>
