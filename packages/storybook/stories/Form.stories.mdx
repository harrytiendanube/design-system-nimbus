import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";
import { Form } from '../../components/src';
import Wrapper from './tsx/FormWrapper.tsx';

import '../utils/style.css';

<Meta title="Atoms/Form" component={Form} />

# Form

## detail component

Other description

<Preview>
  <Story name="standard" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Wrapper
    submitText="Login"
    submitCallback={() => alert("Login click")}
    optionalButtonText="Cancelar"
    optionalCallback={() => alert("Cancel")} >
    {text('Text','Texto Inicial')}
    </Wrapper>
  </Story>
</Preview>

## History X

<Preview withToolbar>
  <Story name="clases">
  <Form
    submitText="Login"
    submitCallback={() => alert("Login click")}
    optionalButtonText="Cancelar"
    optionalCallback={() => alert("Cancel")}
  >
    Texto Inical
  </Form>
  </Story>
</Preview>

<Props of={Form} />
  