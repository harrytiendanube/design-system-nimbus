import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";
import { Form, Input } from '../../components/src';

import '../utils/style.css';

<Meta title="Atoms/Form" component={Form} />

# Form

## detail component

Other description

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Form
    titleText="Titulo"
    subtitleText="Subtitulo"
    submitText="Primary"
    alertText="Validation submit mesesage"
    alertAppearance="danger"
    linkText="Primary text link"
    linkHref="http://www.tiendanube.com"
    submitText="Primary"
    submitCallback={() => alert("Login click")}
    optionalButtonText="Default"
    optionalCallback={() => alert("Cancel")} >
    <Input
      label="label input A"
      value="value input A"
      placeholder="placeholder A"
      name="name A"
    />
    <Input
      label="label input B"
      value="value input B"
      placeholder="placeholder B"
      name="name B"
    />
    </Form>
  </Story>
</Preview>

## History X

<Preview withToolbar>
  <Story name="clases">
  <Form
    alertText="Alert de validación"
    submitText="Login"
    submitCallback={() => alert("Login click")}
    optionalButtonText="Cancelar"
    optionalCallback={() => alert("Cancel")}
  >
  </Form>
  </Story>
</Preview>

<Props of={Form} />
  