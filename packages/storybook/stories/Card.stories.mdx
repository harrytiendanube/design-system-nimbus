import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";
import { Card, Text } from "@tiendanube/components/src";

import { PrinterIcon, CreditCardIcon } from "@tiendanube/icons";

import "../utils/style.css";

<Meta title="Components/Card" component={Card} />

# Card

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <div className="separate">
      <Card
        title={text("title", "Card Title")}
        primaryButton={{
          children: text("primaryButton.children", "Primary"),
          onClick: action("onClickPrimary"),
          disabled: boolean("primaryButton.disabled", false),
          spinner: boolean("primaryButton.spinner", false),
        }}
        secondaryButton={{
          children: text("secondaryButton.children", "Secondary"),
          onClick: action("onClickSecondary"),
          disabled: boolean("secondaryButton.disabled", false),
          spinner: boolean("secondaryButton.spinner", false),
        }}
        headerLabel={{
          id: "header-label-id",
          label: "Header label",
          appearance: "warning",
          icon: CreditCardIcon,
        }}
      >
        <Text>Card content</Text>
      </Card>
    </div>
  </Story>
</Preview>

## Properties

<Props of={Card.type} />

# Skeleton

<Preview>
  <Story name="skeleton" parameters={{ decorators: [withKnobs, withA11y] }}>
    <div className="separate">
      <Card
        title={text("title", "Card Title")}
        primaryButton="skeleton"
        secondaryButton="skeleton"
        headerLabel="skeleton"
      >
        <Text.Skeleton />
      </Card>
    </div>
  </Story>
</Preview>

## Collapsible

TODO: complete

<Preview>
  <Story name="collapsible" parameters={{ decorators: [withKnobs, withA11y] }}>
    <div className="separate">
      <Card
        title="Card Title"
        primaryButton={{
          children: "Primary",
          onClick: action("onClickPrimary"),
        }}
        secondaryButton={{
          children: "Secondary",
          onClick: action("onClickSecondary"),
        }}
        isCollapsible
      >
        <Text>Card content</Text>
      </Card>
    </div>
  </Story>
</Preview>

## Label

TODO: complete

<Preview>
  <Story name="icon" parameters={{ decorators: [withKnobs, withA11y] }}>
    <div className="separate">
      <Card
        title="Card Title"
        primaryButton={{
          children: "Primary",
          onClick: action("onClickPrimary"),
        }}
        secondaryButton={{
          children: "Secondary",
          onClick: action("onClickSecondary"),
        }}
        headerIcon={PrinterIcon}
        onClickHeaderIcon={action("onClickHeaderIcon")}
      >
        <Text>Card content</Text>
      </Card>
    </div>
  </Story>
</Preview>
