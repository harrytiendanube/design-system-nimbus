import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, color, select, text } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { action } from "@storybook/addon-actions";
import centered from '@storybook/addon-centered/react';

import { Input } from '@tiendanube/components/src';
import { LOREM_STRESS } from "./utils/constants";

import '../utils/style.css';

<Meta title="Atoms/Input" component={Input} />

# Input

TODO: complete

<Preview>
  <Story
    name="default"
    parameters={{ decorators: [withKnobs, withA11y, centered] }}
  >
    <Input
      label={text("label","Email o URL de tu tienda")}
      placeholder={text("placehoder","placehoder")}
      value={text("value","")}
      name="email"
      onChange={action("onChange")}
    />
  </Story>
</Preview>

## Properties

<Props of={Input.type} />

## With value

TODO: complete

<Preview withToolbar>
  <Story
    name="with value"
  >
    <Input
      label="Email o URL de tu tienda"
      value="nimbus@tiendanube.com"
      placeholder="example@mail.com"
      name="email"
    />
  </Story>
</Preview>

## Without value

TODO: complete

<Preview withToolbar>
  <Story
    name="without value"
  >
    <Input
      label="Email o URL de tu tienda"
      name="email"
      placeholder="example@mail.com"
    />
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview withToolbar>
  <Story
    name="stressed"
  >
    <Input
      label={LOREM_STRESS}
      name="email"
      placeholder="example@mail.com"
    />
  </Story>
</Preview>