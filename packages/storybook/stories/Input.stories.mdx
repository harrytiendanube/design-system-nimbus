import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { action } from "@storybook/addon-actions";

import { Input } from "@tiendanube/components/src";
import { LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atoms/Input" component={Input} />

# Input

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Input
      label={text("label", "Email")}
      placeholder={text("placeholder", "placeholder")}
      value={text("value", "")}
      name="email"
      onChange={action("onChange")}
    />
  </Story>
</Preview>

## Properties

<Props of={Input.type} />

## With value

TODO: complete

<Preview withToolbar>
  <Story name="with value">
    <Input
      label="Email"
      value="nimbus@tiendanube.com"
      placeholder="example@mail.com"
      name="email"
    />
  </Story>
</Preview>

## Without value

TODO: complete

<Preview withToolbar>
  <Story name="without value">
    <Input label="Email" name="email" placeholder="example@mail.com" />
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview withToolbar>
  <Story name="stressed">
    <Input label={LOREM_STRESS} name="email" placeholder="example@mail.com" />
  </Story>
</Preview>
