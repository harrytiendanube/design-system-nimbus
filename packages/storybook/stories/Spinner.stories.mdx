import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";
import { Spinner } from "@tiendanube/components/src";

import "../utils/style.css";

<Meta title="Atoms/Spinner" component={Spinner} />

# Spinner

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Spinner
      size={select("size", ["base", "small", "medium", "large"])}
      appearance={select("appearance", ["primary", "secondary", "danger"])}
    />
  </Story>
</Preview>

## Properties Spinner

<Props of={Spinner} />
