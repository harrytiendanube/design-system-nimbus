import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";
import {
  HomeIcon,
  MoneyIcon,
  MenuIcon,
  CashIcon,
  TagIcon,
  MobileIcon,
  LogOutIcon,
} from "@tiendanube/icons";
import {
  NimbusApp,
  BaseMenu,
  Page,
  Text,
  Title,
  NavTabs,
} from "@tiendanube/components/src";

import "../utils/style.css";

<Meta title="Components/NimbusApp" component={NimbusApp} />

# NimbusApp

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <NimbusApp
      menu={
        <BaseMenu
          title="Store nimbus"
          href="http://www.tiendanube.com"
          footer={{
            label: "Close Session",
            icon: LogOutIcon,
            onClick: action("onClick Close"),
          }}
        >
          <BaseMenu.Section>
            <BaseMenu.Item
              icon={HomeIcon}
              onClick={action("onClick Start")}
              active
            >
              Start
            </BaseMenu.Item>
          </BaseMenu.Section>
          <BaseMenu.Section title="Manage">
            <BaseMenu.Item icon={CashIcon} onClick={action("onClick Sales")}>
              Sales
            </BaseMenu.Item>
            <BaseMenu.Item icon={TagIcon} onClick={action("onClick Products")}>
              Products
            </BaseMenu.Item>
          </BaseMenu.Section>
          <BaseMenu.Section title="Enhance">
            <BaseMenu.Item
              icon={MobileIcon}
              onClick={action("onClick Keyboard")}
            >
              Keyboard
            </BaseMenu.Item>
          </BaseMenu.Section>
        </BaseMenu>
      }
      header={<Title>Header</Title>}
      navTabs={
        <NavTabs>
          <NavTabs.Item
            label="Dashboard"
            icon={HomeIcon}
            onClick={action("onClick Start")}
            active
          />
          <NavTabs.Item
            label="Orders"
            icon={MoneyIcon}
            onClick={action("onClick Sales")}
            badge
          />
          <NavTabs.Item
            label="Products"
            icon={TagIcon}
            onClick={action("onClick Products")}
          />
          <NavTabs.Item
            label="Menu"
            icon={MenuIcon}
            onClick={action("onClick Menu")}
          />
        </NavTabs>
      }
    >
      <Page
        title="Page Title"
        headerNavigation={{
          onClick: action("headerNavigation.action.onClick"),
          children: "Back",
        }}
        paginationPrevious={action("paginationPrevious")}
        paginationNext={action("paginationNext")}
        primaryAction={{
          onClick: action("onClickPrimaryAction"),
          children: "Primary action",
        }}
        secondaryActions={[
          {
            children: "Secondary action 1",
            onClick: action("onClickSecondaryAction1"),
          },
          {
            children: "Secondary action 2",
            onClick: action("onClickSecondaryAction2"),
          },
        ]}
        headerLabels={[
          {
            id: "label-id-1",
            label: "label 1",
          },
          {
            id: "label-id-2",
            label: "label 2",
          },
        ]}
      >
        <Text>Page content</Text>
      </Page>
    </NimbusApp>
  </Story>
</Preview>

## Properties NimbusApp

<Props of={NimbusApp} />
