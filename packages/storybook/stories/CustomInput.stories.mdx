import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, boolean } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";

import { InfoCircleIcon } from "@tiendanube/icons";
import { CustomInput } from "@tiendanube/components/src";

import "../utils/style.css";

<Meta title="Atomic components/CustomInput" component={CustomInput} />

# CustomInput

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <CustomInput
      label={text("label", "Label")}
      labelIcon={InfoCircleIcon}
      id={text("id", "Input ID")}
      focus={boolean("focus", false)}
      disabled={boolean("disabled", false)}
    >
      <input
        style={{ display: "block", width: "100%" }}
        placeholder="custom input children"
      />
    </CustomInput>
  </Story>
</Preview>

## Properties

<Props of={CustomInput} />
