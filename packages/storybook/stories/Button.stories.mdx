import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";

import { Button } from '../../components/src';
import '../utils/style.css'

<Meta title="Atoms/Button" component={Button} />

# Button

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Button
      start="Facebook"
      end="LongArrowRight"
      appearance={select("Color", ["default", "secondary", "light"])}
      onClick={action('onClick')}
    >
      {text("Text", "Text")}
    </Button>
  </Story>
</Preview>

## Properties

<Props of={Button} />

## Colores

TODO: complete

<Preview withToolbar>
  <Story name="clases">
    <div className="separate">
      <Button appearance="primary"> Primary </Button>
      <Button appearance="secondary"> Secondary </Button>
      <Button appearance="light"> Light </Button>
      <Button appearance="danger"> Danger </Button>
      <Button appearance="transparent"> Transparent </Button>
    </div>
  </Story>
</Preview>

## Outline

TODO: complete

<Preview withToolbar>
  <Story name="outline">
    <div className="separate">
      <Button appearance="primary" outline={true}>Primary</Button>
      <Button appearance="secondary" outline={true}>Secondary</Button>
      <Button appearance="danger" outline={true}>Danger</Button>
    </div>
  </Story>
</Preview>





