import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";
import centered from '@storybook/addon-centered/react';

import { Button } from '@tiendanube/components/src';
import { LOREM_STRESS } from "./utils/constants";

import '../utils/style.css';

<Meta title="Atoms/Button" component={Button} />

# Button

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y, centered] }}>
    <Button
      start="Facebook"
      end="LongArrowRight"
      appearance={select("Color", ["light", "primary", "secondary", "danger", "success"])}
      onClick={action('onClick')}
    >
      {text("Text", "Text")}
    </Button>
  </Story>
</Preview>

## Properties

<Props of={Button.type} />

## Appearance

TODO: complete

<Preview withToolbar>
  <Story name="appearance">
    <div className="separate">
      <Button appearance="light"> Light </Button>
      <Button appearance="primary"> Primary </Button>
      <Button appearance="secondary"> Secondary </Button>
      <Button appearance="danger"> Danger </Button>
      <Button appearance="success"> Success </Button>
    </div>
  </Story>
</Preview>

## Outline

TODO: complete

<Preview withToolbar>
  <Story name="outline">
    <div className="separate">
      <Button appearance="primary" outline>Primary</Button>
      <Button appearance="secondary" outline>Secondary</Button>
      <Button appearance="danger" outline>Danger</Button>
    </div>
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview withToolbar>
  <Story name="stressed">
      <Button>{LOREM_STRESS}</Button>
  </Story>
</Preview>




