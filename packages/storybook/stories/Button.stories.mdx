import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text, boolean } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";

import { Button } from "@tiendanube/components/src";
import { ArrowRightIcon, ExternalLinkIcon } from "@tiendanube/icons";
import { LOREM_STRESS } from "./utils/constants";

import "../utils/style.css";

<Meta title="Atoms/Button" component={Button} />

# Button

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Button
      icon={ArrowRightIcon}
      iconPosition={select("iconPosition", ["start", "end"])}
      appearance={select("appearance", [
        "default",
        "primary",
        "secondary",
        "danger",
      ])}
      disabled={boolean("disabled", false)}
      onClick={action("onClick")}
    >
      {text("Text", "Text")}
    </Button>
  </Story>
</Preview>

## Properties

<Props of={Button.type} />

## Appearance

TODO: complete

<Preview withToolbar>
  <Story name="appearance">
    <div>
      <div className="separate">
        <Button appearance="default"> Default </Button>
        <Button appearance="primary"> Primary </Button>
        <Button appearance="secondary"> Secondary </Button>
        <Button appearance="danger"> Danger </Button>
      </div>
      <br />
      <div className="separate">
        <Button appearance="default" disabled>
          Default
        </Button>
        <Button appearance="primary" disabled>
          Primary
        </Button>
        <Button appearance="secondary" disabled>
          Secondary
        </Button>
        <Button appearance="danger" disabled>
          Danger
        </Button>
      </div>
    </div>
  </Story>
</Preview>

## Stressed

TODO: complete

<Preview withToolbar>
  <Story name="stressed">
    <Button icon={ExternalLinkIcon} iconPosition="end">
      {LOREM_STRESS}
    </Button>
  </Story>
</Preview>

## Link Styling

TODO: complete

<Preview withToolbar>
  <Story name="link styling">
    <div>
      <div className="separate">
        <Button
          appearance="default"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
        >
          {" "}
          Default{" "}
        </Button>
        <Button
          appearance="primary"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
        >
          {" "}
          Primary{" "}
        </Button>
        <Button
          appearance="secondary"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
        >
          {" "}
          Secondary{" "}
        </Button>
        <Button
          appearance="danger"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
        >
          {" "}
          Danger{" "}
        </Button>
      </div>
      <br />
      <div className="separate">
        <Button
          appearance="default"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
          disabled
        >
          Default
        </Button>
        <Button
          appearance="primary"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
          disabled
        >
          Primary
        </Button>
        <Button
          appearance="secondary"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
          disabled
        >
          Secondary
        </Button>
        <Button
          appearance="danger"
          icon={ExternalLinkIcon}
          iconPosition="end"
          link
          disabled
        >
          Danger
        </Button>
      </div>
    </div>
  </Story>
</Preview>
