import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from '@storybook/addon-actions';
import { withA11y } from "@storybook/addon-a11y";

import { Button } from '../../components/src';
import '../utils/style.css'

<Meta title="Atoms/Button" component={Button} />

# Button

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <Button
      start="Facebook"
      end="LongArrowRight"
      apparence={select("Color", ["default", "secondary", "light"])}
      onClick={action('onClick')}
    >
      {text("Text", "Text")}
    </Button>
  </Story>
</Preview>

## Properties

<Props of={Button} />

## Colores

TODO: complete

<Preview withToolbar>
  <Story name="clases">
    <div className="separate">
      <Button apparence="primary"> Primary </Button>
      <Button apparence="secondary"> Secondary </Button>
      <Button apparence="light"> Light </Button>
      <Button apparence="danger"> Danger </Button>
      <Button apparence="transparent"> Transparent </Button>
    </div>
  </Story>
</Preview>

## Outline

TODO: complete

<Preview withToolbar>
  <Story name="outline">
    <div className="separate">
      <Button apparence="primary" outline={true}>Primary</Button>
      <Button apparence="secondary" outline={true}>Secondary</Button>
      <Button apparence="danger" outline={true}>Danger</Button>
    </div>
  </Story>
</Preview>





