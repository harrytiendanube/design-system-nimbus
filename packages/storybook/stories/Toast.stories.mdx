import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, text, select } from "@storybook/addon-knobs";
import { withA11y } from "@storybook/addon-a11y";

import { Toast, Button } from "@tiendanube/components/src";

<Meta title="Components/Toast" component={Toast} />

# Toast

TODO: complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    {() => {
      const [active, setActive] = React.useState(false);
      const toggleActive = () => setActive((currentActive) => !currentActive);
      return (
        <>
          <Button onClick={toggleActive}>Show Toast</Button>
          {active && (
            <Toast
              label={text("label", "label")}
              appearance={select("appearance", [
                "primary",
                "secondary",
                "success",
                "warning",
                "danger",
              ])}
              onClose={toggleActive}
            />
          )}
        </>
      );
    }}
  </Story>
</Preview>

## Properties

<Props of={Toast.type} />
