import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";
import { withKnobs, select, text } from "@storybook/addon-knobs";
import { action } from "@storybook/addon-actions";
import { withA11y } from "@storybook/addon-a11y";

import { CalloutCard } from "@tiendanube/components/src";

import { BoxUnpackedIcon } from "@tiendanube/icons";

<Meta title="Components/CalloutCard" component={CalloutCard} />

# CalloutCard

TODO: Complete

<Preview>
  <Story name="default" parameters={{ decorators: [withKnobs, withA11y] }}>
    <CalloutCard
      id="myId"
      appearance={select(
        "appearance",
        ["primary", "secondary", "danger", "warning", "success"],
        "primary",
      )}
      icon={BoxUnpackedIcon}
      title={text("title", "title")}
      subtitle={text("subtitle", "subtitle")}
      onClick={action("onClick")}
    />
  </Story>
</Preview>

## Properties

<Props of={CalloutCard} />
